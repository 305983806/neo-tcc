dependencies {
    compile project (":neo-tcc-core")
    implementation "org.springframework:spring-context:${SPRING}"
    implementation "org.quartz-scheduler:quartz:${QUARTZ}"
    implementation "org.aspectj:aspectjweaver:${ASPECTJWEAVER}"
    implementation "org.springframework:spring-context-support:${SPRING}"
}